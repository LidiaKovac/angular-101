import { CustomPasswordDirective } from './custom-password.validator';

describe('CustomPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
